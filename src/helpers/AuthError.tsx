import React from 'react'

type Props = {
    data:any
}

export default function AuthError({data}: Props) {
    //this takes in the error response and displays it 
  return (
    <div>AuthError</div>
  )
}